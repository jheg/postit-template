<% header = link_to('All Posts', posts_path) + "&raquo; #{@the_category.name}".html_safe %>
<%= render 'shared/content_title', title: header  %>

  <% @the_category.posts.each do |the_post| %>
    <%= render 'posts/post', the_post: the_post %>
  <% end %>
